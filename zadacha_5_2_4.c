#include <stdio.h>

int main()
{
  int x1, x2, x3, x4, x5;
  scanf("%d %d %d %d %d", &x1, &x2, &x3, &x4, &x5);

  printf("%d", (x1 * x1) + (x2 * x2) + (x3 * x3) + (x4 * x4) + (x5 * x5));

  return 0;
}
/*#include <stdio.h>

int main() {
  int k1, k2, k3, k4, k5;
  scanf("%d%d%d%d%d", &k1, &k2, &k3, &k4, &k5);
  printf("%d", k1*k1 + k2*k2 + k3*k3 + k4*k4 + k5*k5);
  return 0;
}*/